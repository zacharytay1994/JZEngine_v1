#pragma once
#include "../ECS/ECS.h"

namespace JZEngine
{
	/*struct Selectable
	{
		bool selected = true;
	};

	struct SelectableSystem : public ECS::System
	{
		SelectableSystem();

		virtual void Update(const float& dt) override;
	};*/
}